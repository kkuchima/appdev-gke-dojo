apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: frontend-route
spec:
  parentRefs:
  - name: external-http
  hostnames:
  - "34-36-240-216.nip.io"
  rules:
  - matches:
    - path:
        value: /
    backendRefs:
    - name: frontend
      port: 80